# Milestone sheet — K0: Specification, threat model, scaffold

- **Owner:** kovee
- **Prerequisites:** C0 (done — D1/D9/D10 amendments merged first)
- **Operation/schema bundle:** the pinned kovee §11.6 `core_v1`,
  `shared_space_v1`, `developer_assistant_v1` command/query sets and the
  minimal worker protocol, **resolved together with amendment A5's
  wire-name table into one registry** (no Sage-era wire name survives:
  `governance_enable/show/disable`, `endeavor_promotion_*`,
  `byom_episode_binding_show`); the K0-frozen operation registry (operation ×
  surface × actor × AuthorizationDependencySet categories) is the source of
  all later counts
- **Produced artifacts:**
  - repo scaffold mirroring akson M0: workspace (`kovee-core`, `kovee-store`,
    `kovee-auth`, `kovee-space`, `kovee-attention`, `kovee-commitment`,
    `kovee-runtime`, `kovee-effects`, `kovee-artifacts`, `kovee-bus`,
    **`kovee-byom`**, `kovee-akson`, `koveed`, `kovee-cli` — per kovee §24
    with A1's substitution), pinned toolchain, `deny.toml`, CI (check / deny /
    xcheck / tscheck / proof skeleton), `spec/` + `spec/adr/` + `reviews/`
  - JSON Schemas + golden vectors. **Delivered so far:** the five generic
    envelope schemas (command/result/event/problem/hello, incl. the closed
    read shape), envelope schema/acceptance/digest vectors covering every
    §11.8 and family cap with boundaries and negatives, and the `xcheck/`
    (Python) + `tscheck/` (TypeScript) re-derivers; limits/errors/digest
    vectors aligned to C1 (typed artifact digest classes per amendment A5 —
    no retained plaintext `raw_sha256`). **NOT yet delivered (remaining K0
    work, required before RT):** the per-operation command/result schema
    suite for the three K1 bundles, their per-operation negative matrices
    (wrong surface, wrong actor, dependency invalidation, replay), and the
    TypeScript coverage of those per-operation vectors (R0 KENV-01
    disposition: tracked to K0 completion)
  - threat model naming byom (not Sage) as governance owner
  - amendment A1–A5 text folded into the extracted spec
  - ADRs: K-ADR-1 (stack), K-ADR-2 (web UI) opened; D-decision ADRs recorded
- **Migrations:** initial SQLite migration scaffold (numbered, akson-store
  convention); no domain tables yet beyond K1's
- **Assurance profile:** developer only (declared in every artifact)
- **Verification:** `cargo test --workspace --locked` green on the scaffold;
  `python3 xcheck/run.py spec/vectors` and `npm test --prefix tscheck`
  re-derive all K0 vectors including negatives; C1 family-vectors job wired;
  registry parity check (`cargo test -p kovee-core --test registry_parity`:
  the registry equals the independently frozen exact `(bundle, operation,
  surface)` set — one row per expected (operation, surface) clause, so the
  four dual-surface operations each have two rows — plus closed field
  enums; no unresolved Sage-era name)
- **Review evidence:** R0 covers the spec extraction (L1, L4)
- **Size:** M
- **Integration gate relation:** none (K1 feeds I0)
