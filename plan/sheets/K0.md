# Milestone sheet — K0: Specification, threat model, scaffold

- **Owner:** kovee
- **Prerequisites:** C0 (ratified — D1/D9/D10 amendments merged first)
- **Operation/schema bundle (from kovee §11.6, frozen into `kovee/spec/` here):**
  `core_v1`, `shared_space_v1`, `developer_assistant_v1` command/query sets and
  the minimal worker protocol — extracted verbatim from the pinned DESIGN.md
  into machine-readable schemas; the K0-frozen operation registry (operation ×
  surface × actor × AuthorizationDependencySet categories) is the source of all
  later counts
- **Produced artifacts:**
  - repo scaffold mirroring akson M0: workspace (`kovee-core`, `kovee-store`,
    `kovee-auth`, `kovee-space`, … per kovee §24, with **`kovee-byom`** in
    place of `kovee-sage`), pinned toolchain, `deny.toml`, CI (check / deny /
    xcheck / proof skeleton), `spec/` + `spec/adr/` + `reviews/`
  - JSON Schemas + golden vectors for the three K1 bundles; limits/errors/
    digest vectors aligned to C1's family profile; `xcheck/` (Python) and
    `tscheck/` (TypeScript) re-derivers
  - threat model naming byom (not Sage) as governance owner
  - ADRs: K-ADR-1 (stack), K-ADR-2 (web UI) opened; D-decision ADRs recorded
- **Migrations:** initial SQLite schema migration scaffold (numbered, per
  akson-store convention); no domain tables yet beyond K1's
- **Assurance profile:** developer only (declared in every artifact)
- **Verification:** CI green on empty crates; `xcheck`/`tscheck` re-derive all
  K0 vectors; C1 family-vectors job wired; registry structural check (every
  bundle op has exactly one registry row)
- **Review evidence:** R0 covers the spec extraction (L1, L4)
- **Size:** M
- **Integration gate relation:** none (K1 feeds I0)
