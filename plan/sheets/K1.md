# Milestone sheet — K1: Personal shared-space wedge

- **Owner:** kovee
- **Prerequisites:** K0 (exit), C1 (contract-ready)
- **Operation/schema bundle:** the K0-frozen `core_v1` + `shared_space_v1` +
  `developer_assistant_v1` registries — one realm/project/space, participants,
  typed contributions (`utterance|goal|question|claim|observation|evidence|
  proposal|critique|synthesis|result|decision_reference|system_notice`) and
  relations (`addresses|supports|challenges|refines|qualifies|supersedes|
  depends_on|derived_from|produced_by|quotes|evaluates`), pinned
  `SpaceFrontier`s, Stream + Workbench lenses, exact `ContextAssembly` with the
  built-in `explicit_refs_v1` selection policy, event ledger with dense
  sequences and opaque cursors, artifacts (local content-addressed), local
  Unix-socket binding, thin CLI
- **Explicitly out:** models, NATS, isolation claims, attention, branches,
  commitments, governance (byomd not required to run K1's own suite)
- **Produced artifacts:** `koveed` (personal profile) + `kovee` CLI + Python
  SDK worker via authenticated one-shot direct invocation; the deterministic
  acceptance assistant (kovee §26 K1 — **no model dependency**); numbered
  SQLite migrations for the K1 domain tables
- **Assurance profile:** developer (labeled in `EnforcementEvidence` fields as
  unclaimed confinement)
- **Verification (test IDs):**
  - `k1-acceptance`: init → question → invoke → **kill koveed mid-flow** →
    restart → exactly one synthesis + `addresses` relation, no duplication
    (kovee §26 K1 exit, verbatim)
  - `k1-crash-matrix`: kill-and-restart at each command-transaction commit
    point; idempotent retry returns byte-identical results
  - `k1-vectors`: xcheck + tscheck re-derive all envelope/digest/cursor vectors
  - `k1-no-authority`: hostile contributions cannot select actors, wake
    anything, or widen anything (kovee §20.3 subset)
- **Review evidence:** R1 (after I0) — L1, L2, L3
- **Size:** L
- **Integration gate relation:** feeds I0 (with B1 + C3a)
