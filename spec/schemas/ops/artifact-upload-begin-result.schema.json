{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://kovee.example/kcp/v0/ops/artifact-upload-begin-result.schema.json",
  "title": "KCP artifact_upload_begin result (DESIGN.md §10.10, §11.6.1 space/content mutation family)",
  "description": "CommandResult.result payload for artifact_upload_begin (§11.2 ok arm; the envelope's revision/event_cursor members live outside this payload). The §10.10 spelled narrow canonical projection — only the durable upload/artifact refs and constraints, never a credential (gap note KG29); the separate non-mutating artifact_upload_credential query issues staging credentials.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "upload_id",
    "artifact_id",
    "revision",
    "state",
    "declared_raw_sha256",
    "declared_size",
    "declared_media_type",
    "classification_ref",
    "max_bytes",
    "expires_at",
    "created_at"
  ],
  "properties": {
    "upload_id": {
      "$ref": "#/$defs/identifier"
    },
    "artifact_id": {
      "$ref": "#/$defs/identifier"
    },
    "revision": {
      "$ref": "#/$defs/safeNonNegativeInteger"
    },
    "state": {
      "$ref": "#/$defs/uploadState"
    },
    "declared_raw_sha256": {
      "$ref": "#/$defs/rawSha256"
    },
    "declared_size": {
      "$ref": "#/$defs/safeNonNegativeInteger"
    },
    "declared_media_type": {
      "$ref": "#/$defs/mediaType"
    },
    "classification_ref": {
      "$ref": "#/$defs/identifier"
    },
    "max_bytes": {
      "$ref": "#/$defs/safeNonNegativeInteger"
    },
    "expires_at": {
      "$ref": "#/$defs/timestamp"
    },
    "created_at": {
      "$ref": "#/$defs/timestamp"
    }
  },
  "$defs": {
    "identifier": {
      "type": "string",
      "pattern": "^[\\x21-\\x7e]{1,128}$",
      "description": "Opaque identifier, at most 128 bytes (§11.8); visible-ASCII alphabet is a K0 extraction decision (spec/schemas/README.md)."
    },
    "mediaType": {
      "type": "string",
      "pattern": "^[!#$%&'*+.^_`|~a-zA-Z0-9-]+/[!#$%&'*+.^_`|~a-zA-Z0-9-]+$",
      "maxLength": 128,
      "description": "RFC 6838 type/subtype token pair (media_type/declared_media_type); parameters are not carried in these fields — recorded extraction bound (gap note KG21/KG29)."
    },
    "rawSha256": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$",
      "description": "Ordinary raw SHA-256 content checksum (raw_sha256/declared_raw_sha256, §10.10) for integrity and content-addressing; a different field type from the §11.8 typed digests, never interchangeable with them even when the 32-byte values match (§11.8)."
    },
    "safeNonNegativeInteger": {
      "type": "integer",
      "minimum": 0,
      "maximum": 9007199254740991,
      "description": "I-JSON safe integer (§11.8 rejects unsafe integers)."
    },
    "timestamp": {
      "type": "string",
      "pattern": "^[0-9]{4}-(0[1-9]|1[0-2])-(0[1-9]|[12][0-9]|3[01])T([01][0-9]|2[0-3]):[0-5][0-9]:([0-5][0-9]|60)(\\.[0-9]+)?(Z|[+-]([01][0-9]|2[0-3]):[0-5][0-9])$",
      "format": "date-time",
      "description": "RFC 3339 date-time (recorded K0 extraction decision, spec/schemas/README.md item 7); format: date-time additionally requires semantic calendar validity (R0 KENV-05)."
    },
    "uploadState": {
      "type": "string",
      "enum": [
        "prepared",
        "uploading",
        "sealing",
        "sealed",
        "verifying",
        "completed",
        "rejected",
        "expired",
        "aborted"
      ],
      "description": "Closed ArtifactUpload.state enum, verbatim §10.10 (§11.8: closed)."
    }
  }
}
