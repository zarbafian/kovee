{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://kovee.example/kcp/v0/ops/contribution-show-result.schema.json",
  "title": "KCP contribution_show result (DESIGN.md §10.2, §11.6.1 read family)",
  "description": "CommandResult.result payload for contribution_show (§11.2 ok arm; the envelope's revision/event_cursor members live outside this payload). The visible Contribution projection (gap notes KG4/KG21).",
  "$ref": "#/$defs/contribution",
  "type": "object",
  "$defs": {
    "artifactPart": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "artifact_ref"
      ],
      "properties": {
        "artifact_ref": {
          "$ref": "#/$defs/identifier"
        },
        "title": {
          "$ref": "#/$defs/displayName"
        }
      },
      "description": "ArtifactPart {artifact_ref, title?} (§10.2); nothing may reference an artifact as available before finalization completes (§10.10)."
    },
    "contribution": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "contribution_id",
        "revision",
        "realm_id",
        "project_id",
        "space_id",
        "origin_branch_id",
        "origin_branch_sequence",
        "space_sequence",
        "author_actor_ref",
        "kind",
        "schema_ref",
        "body_parts",
        "subject_refs",
        "source_refs",
        "classification_ref",
        "retention_policy_ref",
        "content_digest",
        "created_at"
      ],
      "properties": {
        "contribution_id": {
          "$ref": "#/$defs/identifier"
        },
        "revision": {
          "const": 1,
          "description": "Immutable record: fixed revision 1 (§10 preamble; gap note KG30)."
        },
        "realm_id": {
          "$ref": "#/$defs/identifier"
        },
        "project_id": {
          "$ref": "#/$defs/identifier"
        },
        "space_id": {
          "$ref": "#/$defs/identifier"
        },
        "origin_branch_id": {
          "$ref": "#/$defs/identifier"
        },
        "origin_branch_sequence": {
          "$ref": "#/$defs/safeNonNegativeInteger"
        },
        "space_sequence": {
          "$ref": "#/$defs/safeNonNegativeInteger"
        },
        "author_actor_ref": {
          "$ref": "#/$defs/identifier"
        },
        "kind": {
          "$ref": "#/$defs/contributionKind"
        },
        "schema_ref": {
          "$ref": "#/$defs/identifier"
        },
        "body_parts": {
          "type": "array",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/contributionPart"
          }
        },
        "subject_refs": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/identifier"
          }
        },
        "source_refs": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/identifier"
          }
        },
        "epistemic_posture": {
          "$ref": "#/$defs/epistemicPosture"
        },
        "invocation_ref": {
          "$ref": "#/$defs/identifier"
        },
        "context_assembly_ref": {
          "$ref": "#/$defs/identifier"
        },
        "causation_ref": {
          "$ref": "#/$defs/identifier"
        },
        "classification_ref": {
          "$ref": "#/$defs/identifier"
        },
        "retention_policy_ref": {
          "$ref": "#/$defs/identifier"
        },
        "content_digest": {
          "$ref": "#/$defs/digestHex"
        },
        "created_at": {
          "$ref": "#/$defs/timestamp"
        }
      },
      "description": "Contribution projection, field names verbatim from the §10.2 record shape (gap notes KG4/KG21/KG30)."
    },
    "contributionKind": {
      "type": "string",
      "enum": [
        "utterance",
        "goal",
        "question",
        "claim",
        "observation",
        "evidence",
        "proposal",
        "critique",
        "synthesis",
        "result",
        "decision_reference",
        "system_notice"
      ],
      "description": "Closed ContributionKind enum, verbatim §10.2 (§11.8: closed). Only services may append a system_notice (§10.4) — a registry/authorization rule, not schema-expressible."
    },
    "contributionPart": {
      "oneOf": [
        {
          "$ref": "#/$defs/textPart"
        },
        {
          "$ref": "#/$defs/dataPart"
        },
        {
          "$ref": "#/$defs/artifactPart"
        },
        {
          "$ref": "#/$defs/referencePart"
        },
        {
          "$ref": "#/$defs/mentionPart"
        }
      ],
      "description": "§10.2 ContributionPart union, discriminated structurally by the disjoint closed required member sets of its five arms — no type tag exists in the record model; §14.1's {\"type\": \"text\"} literal is SDK sugar (gap note KG21)."
    },
    "dataPart": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "schema_ref",
        "value"
      ],
      "properties": {
        "schema_ref": {
          "$ref": "#/$defs/identifier"
        },
        "value": {
          "description": "Typed data validated by the schema at schema_ref; any JSON value here (§10.2 DataPart). Unknown extension kinds remain inert data and never create authority (§10.2)."
        }
      },
      "description": "DataPart {schema_ref, value} (§10.2)."
    },
    "digestHex": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$",
      "description": "Lowercase hex SHA-256 value of a §11.8 typed digest; the digest type/domain is fixed by the schema field holding it (§11.8: a bare 32-byte value cannot be substituted across digest fields)."
    },
    "displayName": {
      "type": "string",
      "minLength": 1,
      "maxLength": 256,
      "description": "Display name bounded by the §11.8 cap of 256 Unicode scalar values, length-bounded again at admission."
    },
    "epistemicPosture": {
      "type": "string",
      "enum": [
        "asserted",
        "tentative",
        "observed",
        "reported",
        "contested"
      ],
      "description": "Closed Contribution.epistemic_posture enum, verbatim §10.2 (§11.8: closed)."
    },
    "identifier": {
      "type": "string",
      "pattern": "^[\\x21-\\x7e]{1,128}$",
      "description": "Opaque identifier, at most 128 bytes (§11.8); visible-ASCII alphabet is a K0 extraction decision (spec/schemas/README.md)."
    },
    "inlineText": {
      "type": "string",
      "maxLength": 65536,
      "description": "Contribution inline text content, bounded by the §11.8 64 KiB contribution-inline-content cap; maxLength counts Unicode scalar values and the byte cap is re-checked at admission (gap note KG21)."
    },
    "languageTag": {
      "type": "string",
      "pattern": "^[a-zA-Z]{1,8}(-[a-zA-Z0-9]{1,8})*$",
      "maxLength": 64,
      "description": "BCP 47-shaped language tag for TextPart.language (§10.2 names the field with no encoding; recorded extraction bound, gap note KG21)."
    },
    "mediaType": {
      "type": "string",
      "pattern": "^[!#$%&'*+.^_`|~a-zA-Z0-9-]+/[!#$%&'*+.^_`|~a-zA-Z0-9-]+$",
      "maxLength": 128,
      "description": "RFC 6838 type/subtype token pair (media_type/declared_media_type); parameters are not carried in these fields — recorded extraction bound (gap note KG21/KG29)."
    },
    "mentionPart": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "target_kind",
        "target_ref",
        "target_revision",
        "display_text"
      ],
      "properties": {
        "target_kind": {
          "type": "string",
          "enum": [
            "principal",
            "assistant_alias"
          ],
          "description": "Closed MentionPart.target_kind enum, verbatim §10.2 (§11.8: closed)."
        },
        "target_ref": {
          "$ref": "#/$defs/identifier"
        },
        "target_revision": {
          "$ref": "#/$defs/safeNonNegativeInteger"
        },
        "display_text": {
          "$ref": "#/$defs/displayName"
        }
      },
      "description": "MentionPart (§10.2); a structured mention resolves an exact visible alias/deployment revision and is inert address data without a direct invocation command or an accepted AttentionContract (§10.4)."
    },
    "referencePart": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "object_ref"
      ],
      "properties": {
        "object_ref": {
          "$ref": "#/$defs/identifier"
        },
        "object_revision": {
          "$ref": "#/$defs/safeNonNegativeInteger"
        },
        "digest": {
          "$ref": "#/$defs/digestHex"
        }
      },
      "description": "ReferencePart {object_ref, object_revision?, digest?} (§10.2); every ref points to an object in the same space or an admitted local copy (§10.2)."
    },
    "safeNonNegativeInteger": {
      "type": "integer",
      "minimum": 0,
      "maximum": 9007199254740991,
      "description": "I-JSON safe integer (§11.8 rejects unsafe integers)."
    },
    "textPart": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "media_type",
        "text"
      ],
      "properties": {
        "media_type": {
          "$ref": "#/$defs/mediaType"
        },
        "text": {
          "$ref": "#/$defs/inlineText"
        },
        "language": {
          "$ref": "#/$defs/languageTag"
        }
      },
      "description": "TextPart {media_type, text, language?} (§10.2)."
    },
    "timestamp": {
      "type": "string",
      "pattern": "^[0-9]{4}-(0[1-9]|1[0-2])-(0[1-9]|[12][0-9]|3[01])T([01][0-9]|2[0-3]):[0-5][0-9]:([0-5][0-9]|60)(\\.[0-9]+)?(Z|[+-]([01][0-9]|2[0-3]):[0-5][0-9])$",
      "format": "date-time",
      "description": "RFC 3339 date-time (recorded K0 extraction decision, spec/schemas/README.md item 7); format: date-time additionally requires semantic calendar validity (R0 KENV-05)."
    }
  }
}
