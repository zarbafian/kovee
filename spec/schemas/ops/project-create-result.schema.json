{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://kovee.example/kcp/v0/ops/project-create-result.schema.json",
  "title": "KCP project_create result (DESIGN.md §10.1, §11.6.1 space/content mutation family)",
  "description": "CommandResult.result payload for project_create (§11.2 ok arm; the envelope's revision/event_cursor members live outside this payload). The created Project projection (gap note KG4).",
  "$ref": "#/$defs/project",
  "type": "object",
  "$defs": {
    "displayName": {
      "type": "string",
      "minLength": 1,
      "maxLength": 256,
      "description": "Display name bounded by the §11.8 cap of 256 Unicode scalar values, length-bounded again at admission."
    },
    "identifier": {
      "type": "string",
      "pattern": "^[\\x21-\\x7e]{1,128}$",
      "description": "Opaque identifier, at most 128 bytes (§11.8); visible-ASCII alphabet is a K0 extraction decision (spec/schemas/README.md)."
    },
    "project": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "project_id",
        "realm_id",
        "revision",
        "name",
        "status",
        "default_classification_ref",
        "policy_set_ref",
        "created_by",
        "created_at"
      ],
      "properties": {
        "project_id": {
          "$ref": "#/$defs/identifier"
        },
        "realm_id": {
          "$ref": "#/$defs/identifier"
        },
        "revision": {
          "$ref": "#/$defs/safeNonNegativeInteger"
        },
        "name": {
          "$ref": "#/$defs/displayName"
        },
        "status": {
          "$ref": "#/$defs/projectStatus"
        },
        "default_classification_ref": {
          "$ref": "#/$defs/identifier"
        },
        "policy_set_ref": {
          "$ref": "#/$defs/identifier"
        },
        "created_by": {
          "$ref": "#/$defs/identifier"
        },
        "created_at": {
          "$ref": "#/$defs/timestamp"
        }
      },
      "description": "Project projection, field names verbatim from the §10.1 record shape (gap note KG4)."
    },
    "projectStatus": {
      "type": "string",
      "enum": [
        "active",
        "suspended",
        "archived"
      ],
      "description": "Closed Project.status enum, verbatim §10.1; safety-relevant enums stay closed (§11.8)."
    },
    "safeNonNegativeInteger": {
      "type": "integer",
      "minimum": 0,
      "maximum": 9007199254740991,
      "description": "I-JSON safe integer (§11.8 rejects unsafe integers)."
    },
    "timestamp": {
      "type": "string",
      "pattern": "^[0-9]{4}-(0[1-9]|1[0-2])-(0[1-9]|[12][0-9]|3[01])T([01][0-9]|2[0-3]):[0-5][0-9]:([0-5][0-9]|60)(\\.[0-9]+)?(Z|[+-]([01][0-9]|2[0-3]):[0-5][0-9])$",
      "format": "date-time",
      "description": "RFC 3339 date-time (recorded K0 extraction decision, spec/schemas/README.md item 7); format: date-time additionally requires semantic calendar validity (R0 KENV-05)."
    }
  }
}
