{
  "name": "envelope/command-invalid-meta-unknown-field",
  "description": "actor identity is not client-provided (§11.2: it is already part of the server-side scope); the closed meta shape rejects an actor member",
  "input": {
    "schema": "kcp-command",
    "value": {
      "version": "0.1",
      "op": "space_create",
      "meta": {
        "request_id": "req-0003",
        "idempotency_key": "idem-0003",
        "actor": "anna"
      },
      "realm_id": "realm-0001",
      "args": {
        "title": "Garden"
      }
    }
  },
  "expected": {
    "valid": false,
    "reason": "unknown meta member"
  }
}
