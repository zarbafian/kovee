{
  "name": "ops/artifact-upload-credential-invalid-missing-required",
  "description": "missing required args member upload_id",
  "input": {
    "schema": "artifact-upload-credential-request",
    "value": {
      "version": "0.1",
      "op": "artifact_upload_credential",
      "realm_id": "realm-0001",
      "args": {}
    }
  },
  "expected": {
    "valid": false,
    "reason": "missing required upload_id"
  }
}
