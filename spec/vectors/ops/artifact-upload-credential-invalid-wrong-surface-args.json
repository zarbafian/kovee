{
  "name": "ops/artifact-upload-credential-invalid-wrong-surface-args",
  "description": "wrong-surface args: the byte ceiling was recorded at artifact_upload_begin (§10.10); the credential query cannot restate or widen it (gap note KG29)",
  "input": {
    "schema": "artifact-upload-credential-request",
    "value": {
      "version": "0.1",
      "op": "artifact_upload_credential",
      "realm_id": "realm-0001",
      "args": {
        "upload_id": "upl-0001",
        "max_bytes": 1048576
      }
    }
  },
  "expected": {
    "valid": false,
    "reason": "unknown member max_bytes"
  }
}
