{
  "name": "ops/artifact-upload-finalize-invalid-missing-required",
  "description": "missing required args member upload_id",
  "input": {
    "schema": "artifact-upload-finalize-request",
    "value": {
      "version": "0.1",
      "op": "artifact_upload_finalize",
      "meta": {
        "request_id": "req-0001",
        "idempotency_key": "idem-0001",
        "expected_revision": 4
      },
      "realm_id": "realm-0001",
      "args": {}
    }
  },
  "expected": {
    "valid": false,
    "reason": "missing required upload_id"
  }
}
