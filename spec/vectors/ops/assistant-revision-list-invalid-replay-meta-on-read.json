{
  "name": "ops/assistant-revision-list-invalid-replay-meta-on-read",
  "description": "replay/dependency invalidation: reads do not carry an idempotency key (§11.2, R0 KENV-01); a read carrying replay meta fails the closed read shape",
  "input": {
    "schema": "assistant-revision-list-request",
    "value": {
      "version": "0.1",
      "op": "assistant_revision_list",
      "realm_id": "realm-0001",
      "args": {
        "limit": 50,
        "definition_id": "asstdef-0001"
      },
      "meta": {
        "request_id": "req-0001",
        "idempotency_key": "idem-0001"
      }
    }
  },
  "expected": {
    "valid": false,
    "reason": "no meta member exists on a read"
  }
}
