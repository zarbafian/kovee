{
  "name": "ops/context-assembly-create-invalid-missing-required",
  "description": "missing required args member selection_policy_ref (§10.8: every assembly binds one selection-policy identifier)",
  "input": {
    "schema": "context-assembly-create-request",
    "value": {
      "version": "0.1",
      "op": "context_assembly_create",
      "meta": {
        "request_id": "req-0001",
        "idempotency_key": "idem-0001"
      },
      "realm_id": "realm-0001",
      "project_id": "proj-0001",
      "args": {
        "space_id": "space-0001",
        "branch_id": "branch-0001",
        "audience_ref": "asstdep-0001",
        "purpose": "Summarize the current claim set for the review invocation.",
        "required_refs": [
          "contrib-0007",
          "contrib-0011"
        ]
      }
    }
  },
  "expected": {
    "valid": false,
    "reason": "missing required selection_policy_ref"
  }
}
