{
  "name": "ops/contribution-list-invalid-replay-meta-on-read",
  "description": "replay/dependency invalidation: reads do not carry an idempotency key (§11.2, R0 KENV-01); a read carrying replay meta fails the closed read shape",
  "input": {
    "schema": "contribution-list-request",
    "value": {
      "version": "0.1",
      "op": "contribution_list",
      "realm_id": "realm-0001",
      "project_id": "proj-0001",
      "args": {
        "space_id": "space-0001",
        "branch_id": "branch-0001",
        "kind": "claim",
        "limit": 100
      },
      "meta": {
        "request_id": "req-0001",
        "idempotency_key": "idem-0001"
      }
    }
  },
  "expected": {
    "valid": false,
    "reason": "no meta member exists on a read"
  }
}
