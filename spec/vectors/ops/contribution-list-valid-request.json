{
  "name": "ops/contribution-list-valid-request",
  "description": "space-scoped contribution page narrowed to one branch and kind (gap note KG9)",
  "input": {
    "schema": "contribution-list-request",
    "value": {
      "version": "0.1",
      "op": "contribution_list",
      "realm_id": "realm-0001",
      "project_id": "proj-0001",
      "args": {
        "space_id": "space-0001",
        "branch_id": "branch-0001",
        "kind": "claim",
        "limit": 100
      }
    }
  },
  "expected": {
    "valid": true
  }
}
