{
  "name": "ops/endeavor-promotion-cancel-invalid-wrong-surface-args",
  "description": "wrong-surface args: a cancel cannot claim a tombstone. Only byom installs one over the exact IdempotencyDomain, and Kovee's cancel is admissible ONLY before any send",
  "input": {
    "schema": "endeavor-promotion-cancel-request",
    "value": {
      "version": "0.1",
      "op": "endeavor_promotion_cancel",
      "meta": {
        "request_id": "req-0001",
        "idempotency_key": "idem-0001"
      },
      "realm_id": "realm-0001",
      "args": {
        "formation_id": "efi-0001",
        "reason": "abandoned",
        "tombstone_ref": "tomb-0001"
      }
    }
  },
  "expected": {
    "valid": false,
    "reason": "unknown member tombstone_ref"
  }
}
