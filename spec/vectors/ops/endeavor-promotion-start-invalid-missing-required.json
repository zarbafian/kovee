{
  "name": "ops/endeavor-promotion-start-invalid-missing-required",
  "description": "freshness is not optional: without an authentication observation there is no fresh human authentication attempt to prove, and §16.3 requires one per send",
  "input": {
    "schema": "endeavor-promotion-start-request",
    "value": {
      "version": "0.1",
      "op": "endeavor_promotion_start",
      "meta": {
        "request_id": "req-0001",
        "idempotency_key": "idem-0001"
      },
      "realm_id": "realm-0001",
      "args": {
        "formation_id": "efi-0001"
      }
    }
  },
  "expected": {
    "valid": false,
    "reason": "args.authentication_observation_ref is required"
  }
}
