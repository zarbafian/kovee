{
  "name": "ops/governance-disable-invalid-missing-required",
  "description": "missing required args member confirmed_subject_digest — governance_disable is ALWAYS step-up (frozen authority row)",
  "input": {
    "schema": "governance-disable-request",
    "value": {
      "version": "0.1",
      "op": "governance_disable",
      "meta": {
        "request_id": "req-0001",
        "idempotency_key": "idem-0001"
      },
      "realm_id": "realm-0001",
      "args": {
        "binding_ref": "krbb-0001",
        "expected_owner_revision": 2
      }
    }
  },
  "expected": {
    "valid": false,
    "reason": "missing required confirmed_subject_digest"
  }
}
