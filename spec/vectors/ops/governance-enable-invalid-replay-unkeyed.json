{
  "name": "ops/governance-enable-invalid-replay-unkeyed",
  "description": "replay/dependency invalidation: a mutation whose meta lacks idempotency_key cannot be replay-deduplicated, so the retry-returns-the-identical-binding guarantee has nothing to key on (§11.2)",
  "input": {
    "schema": "governance-enable-request",
    "value": {
      "version": "0.1",
      "op": "governance_enable",
      "meta": {
        "request_id": "req-0001"
      },
      "realm_id": "realm-0001",
      "args": {
        "byom_endpoint_ref": "local",
        "society_ref": "soc-0001",
        "exact_scope_selector": "project:proj-0001",
        "allowed_project_and_space_selectors": [
          "project:proj-0001"
        ],
        "classification_binding_ref": "class-bind-0001",
        "expected_owner_revision": 0
      }
    }
  },
  "expected": {
    "valid": false,
    "reason": "meta.idempotency_key required on every mutation"
  }
}
