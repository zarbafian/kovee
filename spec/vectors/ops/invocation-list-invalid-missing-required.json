{
  "name": "ops/invocation-list-invalid-missing-required",
  "description": "missing required args member limit",
  "input": {
    "schema": "invocation-list-request",
    "value": {
      "version": "0.1",
      "op": "invocation_list",
      "realm_id": "realm-0001",
      "project_id": "proj-0001",
      "args": {
        "state": "running"
      }
    }
  },
  "expected": {
    "valid": false,
    "reason": "missing required limit"
  }
}
