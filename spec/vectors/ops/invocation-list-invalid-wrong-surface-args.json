{
  "name": "ops/invocation-list-invalid-wrong-surface-args",
  "description": "wrong-surface args: worker placement (§10.9) is scheduler state, not a derived filter (gap note KG9)",
  "input": {
    "schema": "invocation-list-request",
    "value": {
      "version": "0.1",
      "op": "invocation_list",
      "realm_id": "realm-0001",
      "project_id": "proj-0001",
      "args": {
        "limit": 50,
        "state": "running",
        "worker_instance_id": "wrk-0001"
      }
    }
  },
  "expected": {
    "valid": false,
    "reason": "unknown member worker_instance_id"
  }
}
