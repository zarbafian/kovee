{
  "name": "ops/project-access-policy-change-prepare-invalid-missing-required",
  "description": "missing required envelope member project_id (the change binds one exact project, §10.1)",
  "input": {
    "schema": "project-access-policy-change-prepare-request",
    "value": {
      "version": "0.1",
      "op": "project_access_policy_change_prepare",
      "meta": {
        "request_id": "req-0001",
        "idempotency_key": "idem-0001",
        "expected_revision": 4
      },
      "realm_id": "realm-0001",
      "args": {
        "proposed_policy_set_ref": "polset-0002"
      }
    }
  },
  "expected": {
    "valid": false,
    "reason": "missing required project_id"
  }
}
