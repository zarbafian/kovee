{
  "name": "ops/project-show-invalid-wrong-surface-args",
  "description": "wrong-surface args: the read target is the envelope project_id (gap note KG16); args is the closed empty object",
  "input": {
    "schema": "project-show-request",
    "value": {
      "version": "0.1",
      "op": "project_show",
      "realm_id": "realm-0001",
      "project_id": "proj-0001",
      "args": {
        "project_id": "proj-0001"
      }
    }
  },
  "expected": {
    "valid": false,
    "reason": "unknown member project_id"
  }
}
