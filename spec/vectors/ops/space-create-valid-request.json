{
  "name": "ops/space-create-valid-request",
  "description": "caller-suppliable §10.2 Space fields with meta; ids, status, and the main branch are server-derived (gap note KG5)",
  "input": {
    "schema": "space-create-request",
    "value": {
      "version": "0.1",
      "op": "space_create",
      "meta": {
        "request_id": "req-0001",
        "idempotency_key": "idem-0001"
      },
      "realm_id": "realm-0001",
      "project_id": "proj-0001",
      "args": {
        "title": "Battery research",
        "visibility": "project",
        "default_classification_ref": "class-internal"
      }
    }
  },
  "expected": {
    "valid": true
  }
}
